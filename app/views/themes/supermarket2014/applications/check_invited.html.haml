%h3 Special application

%p Correct!

- current_user.spaces.each do |space|
  - if  space.applications.where(:year_id => Year.find_by(year: 2021).id).empty?
    %p  Please click below to begin your 2021 application.
    %p= link_to 'Apply to Supermarket 2021 for ' + space.name, '/invited/2021/' + space.slug
  - else
    %p You have already started an application.
    %p= link_to 'Continue/edit your application to Supermarket 2021', edit_application_path(space.applications.where(:year_id => Year.find_by(year: 2021).id).first) 
- if user_signed_in?
  = content_for :sidebar do
    = render :partial => 'layouts/sidebar'   