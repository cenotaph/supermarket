Name,Email,Last Active Year,Initiative,Country
<%= CSV.generate do |csv|
   @users.each do |cs|
     csv << [ cs.display_name, cs.email, cs.last_active_year, cs.initiatives, cs.country ]
   end 
  end %>